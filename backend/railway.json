{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
      "aptPackages": ["ffmpeg"]
    },
    "deploy": {
      "startCommand": "uvicorn app:app --host 0.0.0.0 --port $PORT"
    }
  }